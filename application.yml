# spring:
#   application:
#     name: Config-Default

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

#http://192.168.1.42:8085/application/default
